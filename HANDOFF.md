# 핸드오프

마지막 작업 **2026-09-01** · 드래프트 **2026-10-05** (D-34)

---

## 30초 오리엔테이션

Yahoo Fantasy NBA **14팀 · $200 옥션 · 로스터 9인 · H2H 13캣** 리그의
**오프라인 옥션 진행 장부**. 2025-09 Streamlit 판이 사용성 문제로 실전 미사용 →
2026-09-01 단일 파일 HTML 로 전면 재작성.

| | |
|---|---|
| 실전 파일 | **`console.html`** — 브라우저로 열면 끝. 의존성 0 · 네트워크 0 |
| 총 낙찰 | **126건** (14 × 9) · 종료 조건과 동일 |
| 선수 DB | 690명 · 한글 별칭 147명 · 13캣 성적 |
| 검증 | `./tests/run.sh` → **393건 통과** |
| 리허설 | 작년 실제 120건 **전량 재현** · 건당 1.7초 · 총 3.3분 |
| 계획·진단 | `RENEWAL.md` (왜 이렇게 만들었는지 전부) |
| 사용법 | **툴 안에 내장** (`?` 키). `README.md` 는 그 복사본 |

---

## 드래프트 당일 순서

1. `console.html` 을 브라우저로 연다. 처음이면 사용법이 뜬다.
2. **팀 설정** → 이름 확인. 14팀 시드는 `data/teams.json` 에 있다.
3. **팀 설정 → 지명 순서** → 추첨 결과를 접두사로 입력:
   `병 준 진 경 원 윤 단 지 두 수 철 정 강 완`
   - 리그가 **스네이크**로 정했으면 같은 화면의 체크박스를 켠다 (기본은 단순 순환)
4. 진행. 기본은 **빠른 기록 모드**(낙찰만 찍기), 호가를 따라가려면 `Tab`.
5. 끝나면 **결과 CSV** 와 **세션 저장** 둘 다 내린다.

## 끝난 뒤 (차년도를 위해)

결과 CSV 를 형제 저장소에 넣으면 시장가 캘리브레이션이 그대로 돈다:

```
~/personal_work/nba_fantasy_auction_2026/data/prior_auction_2026_27/results.csv
```

스키마는 작년과 동일하다 (`manager,name_kr,name_en,price,read_confidence` · CRLF).
읽는 쪽이 8곳(`tool/*.py` 7 + `validate.py`)이므로 열을 바꾸면 그쪽이 깨진다.

⚠️ **조인 키는 표기까지 맞아야 한다.** 소비자 정규화는 `NFKD → ascii → lower` 인데
**접미사를 접지 않는다** — `Jimmy Butler` ≠ `Jimmy Butler III`. 그래서 표시명을
`players.json` 표기로 맞춰 뒀다 (빌드 인수 조건으로 고정). 자세히는 RENEWAL 4-5.

---

## 구조

```
console.src.html   원본 (여기를 고친다)
build_db.py        6개 로컬 소스 → data/players.min.json   (stdlib만)
embed.py           원본 + DB → console.html               (단일 파일)
tests/run.sh       전체 검증 393건
```

`console.html` 은 **생성물이다.** 직접 고치면 다음 빌드에 날아간다.

데이터 출처는 전부 `../nba_fantasy_auction_2026` 의 로컬 파일이다 —
BBRef 2시즌 · 실측 혼합 · 작년 옥션 결과. **스크래핑·네트워크 없음.**

### 고칠 때

```
python3 build_db.py && python3 embed.py && ./tests/run.sh
```

---

## 다음 시즌에 반드시 손댈 것

1. **선수 데이터 갱신** — `build_db.py` 의 소스 경로가 `stats_2025_26/` 을 가리킨다.
   차기 시즌 디렉토리로 바꾸고, 2026 신인 3명 수동 등재(`ROOKIE_NAMES`)를 갱신한다.
2. **리그 규격 확인** — 팀 수·로스터·예산이 바뀌면 형제 저장소
   `data/league_settings.json` 을 먼저 고친다. 이 툴은 거기서 읽는다.
3. **`data/teams.json`** 참가자 명단.
4. **지명 규칙** — 2026-09-01 에 "직전 낙찰자" → "순번 로테이션" 으로 바뀌었다.
   확정 사항: **로스터 9명을 채운 팀은 로테이션에서 빠진다** (2026-09-02 사용자 확인).
   **스네이크 여부는 리그 미결** — 툴이 양쪽을 지원하고 `팀 설정` 체크박스로 고른다.
   또 바뀌면 `rules_override` (build_db.py) 와 `nomAtPos()`/`stepPos()` 를 본다.

## 미해결 (이 저장소 밖)

🔴 **형제 저장소 5곳이 낡았다.** 지명 규칙이 순번 로테이션으로 바뀌었는데
`nba_fantasy_auction_2026` 은 "직전 낙찰자" 전제로 남아 있다:

| 위치 | 문제 |
|---|---|
| `data/league_settings.json` | `nominator: "직전 낙찰자"` |
| `docs/01-league-and-format.md:12` | 규칙 서술 |
| **`docs/02-auction-strategy.md:53`** | **§3 전략 절 하나가 통째로 무효** |
| `docs/12-auction-playbook.md:568` | "야후 기본값(순번 로테이션)이 아니다" ← 정반대 |
| `README.md:52` | 동일 |

`tool/auction-console.html` 의 **"태우기 지명 순서"** 기능도 이 전제 위에 있다.
순번 로테이션에서는 지명 타이밍을 스스로 고를 수 없어 전략의 가정이 달라진다.
**드래프트 전에 그쪽에서 정정해야 한다.**

---

## 정보 경계 (깨지 말 것)

`console.html` 은 진행자에게 **파일째로 전달될 수 있고** 화면이 테이블에서 보인다.

| 담는다 | 담지 않는다 |
|---|---|
| 이름 · 팀 · 포지션 · 한글 별칭 | 시장가 밴드 |
| 13캣 성적 · GP · MPG · 출처 | 이탈가 `my_max` |
| 작년 낙찰가 (리그 공유 이력) | 태그 `buy/avoid` · 부상 제외 |

화면에서 감추는 게 아니라 `build_db.py` 단계에서 버린다. 정렬 가중치처럼
**금액이 역산되는 경로**도 서수로 치환했다. 빌드가 유출 0건을 검사한다.

밸류 판단·코어 플랜·피벗은 `../nba_fantasy_auction_2026/tool/auction-console.html` 몫이다.
