# CLAUDE.md

## 이 저장소

14팀 · $200 · 로스터 9인 · H2H 13캣 리그의 **오프라인 옥션 진행 장부**.
진행자(커미셔너)용 단일 파일 HTML. 드래프트 2026-10-05.

먼저 읽을 것: **`HANDOFF.md`** (현황·당일 절차) → `RENEWAL.md` (설계 근거·진단)

## 명령

```bash
python3 build_db.py     # 선수 DB 빌드 → data/players.min.json
python3 embed.py        # + console.src.html → console.html
./tests/run.sh          # 전체 검증 393건 (실전 빌드 + 리허설 빌드)
```

의존성 0. Python stdlib + Node(테스트용)만. **네트워크 접근 없음.**

## 절대 원칙

1. **`console.html` 을 직접 고치지 않는다.** 생성물이다. `console.src.html` 을 고치고
   `embed.py` 를 다시 돌린다.
2. **외부 자원을 끌어오지 않는다.** CDN·웹폰트·이미지·fetch 전부 금지.
   드래프트장에 네트워크가 없다고 가정한다. `tests/offline.mjs` 가 정적으로 막는다.
3. **사적 판단을 임베드하지 않는다.** 시장가·`my_max`·태그는 `build_db.py` 단계에서
   버린다. 이 파일은 진행자에게 넘어가고 화면이 테이블에서 보인다.
   금액이 역산되는 경로(정렬 가중치 등)도 만들지 않는다. 빌드가 검사한다.
4. **라이브 진행 중 낙찰을 막는 건 최소한으로.** 하드 규칙(예산·로스터·중복)만 차단하고
   포지션 같은 불완전한 데이터는 경고만 한다. 오차단이 오기록보다 비싸다.
5. **타이머가 0이 되어도 자동 낙찰하지 않는다.** 사람의 확정을 기다린다.

## 데이터

전부 `../nba_fantasy_auction_2026` 의 로컬 파일에서 온다 — BBRef 2시즌 per-game,
실측 혼합(`measured_full.json`), 작년 옥션 결과(한글 별칭 120건), 리그 설정.
**스크래핑 코드를 다시 쓰지 않는다.** 구 판의 BBRef 스크래퍼는 컬럼 위치를 강제 대입해
조용히 오매핑되는 구조였고, 이미 정제된 CSV 가 옆 저장소에 있다.

## 결과 CSV

작년 `results.csv` 와 스키마·CRLF 가 동일해야 한다. 차년도 시장가 캘리브레이션이
읽고, 소비자가 8곳이다. **조인 키 표기까지 맞아야 한다** — 소비자 정규화는
접미사를 접지 않으므로 표시명은 `players.json` 표기를 따른다 (빌드 인수 조건).

## 테스트

새 기능에는 테스트를 붙인다. 특히:
- 불변식은 **위반 상태를 직접 주입**해 검출을 확인한다 (통과만 보면 동어반복을 놓친다)
- 렌더 변경은 `A.text()`/`A.html()` 로 **실제 출력 문자열**을 본다
- 설명문(내장 사용법)도 필수 항목 존재를 문자열로 검증한다

`ok(..., true)` 처럼 항상 통과하는 검사는 넣지 않는다. 없는 것보다 나쁘다.
