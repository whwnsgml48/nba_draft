# nba_draft 리뉴얼 계획

작성 2026-09-01 · 대상 드래프트 **2026-10-05** (D-34) · 결정 사항 3건은 사용자 확인 완료

---

## 0. 결정된 전제

| 항목 | 결정 |
|---|---|
| 드래프트 진행 | **오프라인/통화 + 커미셔너 수기 기록** |
| 이 툴의 역할 | **커미셔너용 실전 진행 콘솔** (원래 목적 유지) |
| 스택 | **단일 파일 오프라인 HTML** — 의존성 0 · 네트워크 0 · 브라우저만으로 실행 |
| 리그 | 14팀 · $200 · 로스터 9인 · H2H 13캣 · 총 낙찰 **126건** |
| 규칙 | 최소입찰 $1 · **10초 자동 마감** · 지명자 = **순번 로테이션** (2026-09-01 변경, 종전 '직전 낙찰자') · 최대입찰가 = `잔액 − (남은 슬롯 − 1)` |

출처: `~/personal_work/nba_fantasy_auction_2026/data/league_settings.json`

🔴 **그 파일의 `nominator`는 이제 낡았다.** 지명 규칙이 순번 로테이션으로 바뀌었으므로
이 저장소는 `data/players.min.json.rules_override` 로 덮어쓴다. 형제 저장소 정정은 6절 R7 참조.

**스코프 경계 (명문화):**
- 이 툴 = **리그 전체 사실 기록**. "무슨 일이 일어났나."
- `nba_fantasy_auction_2026/tool/auction-console.html` = **내 다음 행동**. "무엇을 해야 하나."
- 밸류 평가 · 코어 플랜 · 피벗 판단은 **전부 후자의 몫**. 이 툴은 침범하지 않는다.

🔴 **경계는 UI가 아니라 데이터 층에서 지킨다** (2026-09-01, 사용자 지적으로 정정).
`console.html` 은 커미셔너에게 **파일째로 전달될 수 있고** 화면이 테이블에서 보인다.
따라서 시장가·`my_max`·태그를 "화면에서 감추는" 것으로는 부족하고, 임베드 데이터에
**애초에 담기지 않아야** 한다. `build_db.py` 가 이를 인수 조건으로 검사한다.

| 담는다 (리그 공유 사실) | 담지 않는다 (사적 판단) |
|---|---|
| 이름 · 팀 · 포지션 · 한글 별칭 | 시장가 밴드 `market_low/high` |
| 13캣 성적 · GP · MPG · 출처 | 이탈가 `my_max` |
| **작년 낙찰가** (커미셔너가 기록한 리그 이력) | 태그 `buy/avoid` · 부상 제외 플래그 |
| 검색 랭킹 **서수** `rk` | 랭킹 가중치 `w` — `1_000_000 + market_high×1000` 이라 **금액이 역산된다** |

마지막 행이 핵심이다. 필드를 지워도 정렬 가중치에 금액이 남아 있으면 유출이다.

---

## 1. 왜 안 쓰였나 — 코드 근거 진단

"사용성이 별로"의 정체는 취향이 아니라 **구조적 결함**이다.

### 1-1. 아키텍처가 라이브 옥션 속도를 낼 수 없다

| # | 문제 | 근거 |
|---|---|---|
| A1 | 클릭 1회 = 서버 왕복 + 화면 전체 리런 | `app.py`에 `st.rerun()` **12곳** |
| A2 | 리런마다 611행 CSV를 5~7회 재파싱 | `load_players()`가 `get_available_players`·`search_players`·사이드바·본문에서 각각 호출 |
| A3 | 낙찰 1건에 CSV 읽기+쓰기 | `data_manager.py:finalize_auction` → `load_players()` → `save_players()` |
| A4 | 입력 포커스가 매 조작마다 소실 | `st.rerun()` 후 위젯 재생성 |

10초 자동 마감 규칙이 있는 옥션에서 **초 단위 응답**이 필요한데, 구조상 불가능하다.

### 1-2. 실전 워크플로와 어긋난다

| # | 문제 | 근거 |
|---|---|---|
| B1 | **되돌리기 없음** — 오타 낙찰의 복구 수단이 "드래프트 전체 초기화"뿐 | `app.py:334` `reset_draft_btn`이 유일 |
| B2 | 선수 선택이 **611개 옵션 selectbox** | `app.py:96-120`, `sorted()` + `index+1` 오프셋 + `delattr` 세션 저글링 |
| B3 | **한글 별칭 검색 불가** — 실전에서 부르는 이름(`웸비` `제이덥` `막윌`)으로 못 찾음 | `search_players()`가 영문 `str.contains`만 |
| B4 | 입찰 1건에 마우스 3회(팀 선택 → 금액 → 버튼) + 리런 대기 | `auction_control_section()` |
| B5 | **10초 타이머 없음** — 리그 핵심 규칙이 툴에 부재 | 전 코드 미구현 |
| B6 | 입찰 히스토리 비영속 | `AuctionManager.bid_history`가 메모리, `state.json` 미포함 |

### 1-3. 리그 규칙이 구현돼 있지 않다

최대입찰가 공식 · 로스터 한도 검증 · 포지션 자격 · 지명 순번 — **4개 전부 없음**.
`max_players_per_team`·`min_bid_increment` 설정값은 저장만 되고 **아무도 읽지 않는다**
(`auction_manager.py:16`이 `= 1` 하드코딩).

### 1-4. 하드코딩

`app.py:377` 12팀 이름 순서 · `/15` 문자열 · `data_manager.py:333` `200 − 잔액` ·
`nba_data.py:18` `'2024-25'` · `basketball_reference.py:15` `season_year='2025'`.

> **결론: 부분 개선이 아니라 재작성.** 살릴 코드는 없다.
> 남길 것은 `data/state.json`의 **12명 팀명 시드**뿐.

### 1-5. 제품적 오진 — 이게 핵심이다

기존 툴은 화면 대부분을 **"경매 시작 → 입찰 → 낙찰" 3단계 강제 플로우**에 썼다.
그런데 커미셔너의 실제 작업량은 **낙찰 확정 126건을 빠르고 정확하게 찍는 것**이다.
호가 경쟁은 구두로 끝나고, 기록해야 하는 건 `선수 + 팀 + 금액` 3요소다.

3단계 플로우를 강제하는 툴은 **한 줄이면 되는 일을 세 화면으로 만들었다.**
그래서 스프레드시트한테 졌다. (작년 실측 기록이 실제로 `.xlsx` 사진으로 남아 있다.)

---

## 2. 설계

### 2-1. 두 모드 — 주/부가 뒤바뀐다

**주 모드: 빠른 기록** (기본값)
> 한 줄 입력 → 즉시 낙찰 확정. `웸비 병욱 87` ⏎

**부 모드: 라이브 호가** (필요할 때만 토글)
> 지명 → 입찰 추적 → **10초 카운트다운** → 낙찰. 큰 숫자 + 소리 + 새 입찰 시 리셋.

기존 툴은 부 모드만 있었다. 그것도 타이머 없이.

### 2-2. 화면

```
┌────────────────────────────────────────────────────────────────┐
│ 지명 병욱 → 원준 → 윤범 | 낙찰 34/126 | 불변식 ✅5/5 [모드 전환] │
├──────────────────────────────┬─────────────────────────────────┤
│ 🔍 [웸비____________]        │  14팀 그리드                    │
│ ┌─ 성적 카드 ──────────────┐ │  ┌────┬────┬────┬────┐          │
│ │ Victor Wembanyama  웸비  │ │  │병욱 │준희 │진빈 │경찬 │          │
│ │ SAS · PF/C · 62G         │ │  │$113│ $54│$166│ $88│  ← 잔액   │
│ │ 작년 $87 · 시장 $90~102  │ │  │max │max │max │max │          │
│ │ PTS REB OREB AST STL BLK │ │  │$106│ $48│$159│ $81│  ← 최대   │
│ │ TOV 3PM FG% 3P% FT% A/T  │ │  │5/9 │7/9 │3/9 │6/9 │  ← 슬롯   │
│ │ DD  MPG      (13캣 전부) │ │  └────┴────┴────┴────┘          │
│ └──────────────────────────┘ │  최대입찰가 미달 팀은 흐리게      │
│  1 Victor Wembanyama SAS C   │                                 │
│  2 ...                       │  │$113│ $54│$166│ $88│  ← 잔액   │
│                              │  │max │max │max │max │          │
│  [팀 ▸ 금액] 확정 ⏎          │  │$106│ $48│$159│ $81│  ← 최대   │
│                              │  │5/9 │7/9 │3/9 │6/9 │  ← 슬롯   │
│  ── 라이브 호가 모드일 때 ──  │  │PG SG SF - C│...│           │
│      ⏱ 7                     │  └────┴────┴────┴────┘          │
│      현재가 $34 · 병욱        │  최대입찰가 미달 팀은 흐리게      │
├──────────────────────────────┴─────────────────────────────────┤
│ 낙찰 로그 (역순, 각 항목 [취소])                                 │
│ 34. Wembanyama → 병욱 $87  [취소]                               │
└────────────────────────────────────────────────────────────────┘
```

큰 글씨 기본 — 테이블 건너 읽히거나 화면 공유가 가능해야 한다.

### 2-3. 키보드 우선

| 키 | 동작 |
|---|---|
| 타이핑 | 즉시 검색 (포커스 항상 검색창) |
| `1`~`5` | 검색 결과 n번째 선택 |
| `Space` | 팀 필드로 이동 → 팀도 타이핑 검색 |
| `Enter` | 확정 |
| `Esc` | 입력 취소 |
| `Ctrl/Cmd+Z` | **되돌리기 (무제한)** |
| `Ctrl/Cmd+Shift+Z` | 다시 실행 |
| `Tab` | 모드 전환 |

목표: **건당 5초 이내, 마우스 0회.**

### 2-4. 검색 — 한글 별칭이 최대 레버

`prior_auction_2025_26/results.csv`에 **작년 실사용 한글 별칭 120건**이 그대로 있다.
`웸비` `요키치` `제이덥` `퍼들` `막윌` `트머삼` `마포주` `자잭주` `SGA` `KAT` `AD` `KD`.

매칭 순서:
1. 한글 별칭 완전일치 → 2. 한글 별칭 부분일치 → 3. 한글 **초성** (`ㅇㅂ` → 웸비)
→ 4. 영문 부분일치 → 5. 영문 성(姓) → 6. 팀 약어 필터

### 2-5. 상태와 영속성

- **localStorage 자동 저장** — 모든 변경 즉시. 브라우저 크래시/새로고침 무손실
- **세션 JSON 내보내기/불러오기** — 기기 교체·백업
- **결과 CSV 내보내기** — 스키마를 작년 `results.csv`와 **동일하게**
  (`manager,name_kr,name_en,price`) → 차년도 시장가 캘리브레이션에 **그대로 투입**
- 되돌리기는 **전체 상태 스냅샷 스택** (126건이면 메모리 무부담)

### 2-6. 불변식 상시 검증

`nba_fantasy_auction_2026/validate.py` 문화를 계승. 화면 우상단에 `✅ 5/5` 상시 표시,
위반 시 빨간 배너.

| ID | 불변식 | 등급 |
|---|---|---|
| I1 | **장부 정합** — 로스터 ↔ 로그 금액·인원 대조 · 지출 ≤ $200 | 위반 |
| I2 | 팀별 낙찰 ≤ 9 · 총 ≤ 126건 | 위반 |
| I3 | 각 낙찰가 ≥ $1 · ≤ **낙찰 당시** 최대입찰가 (로그 시간순 재생) | 위반 |
| I4 | 선수 중복 낙찰 0 | 위반 |
| I5 | PG·SG·SF·PF·C 라인업 실현 가능 (이분 매칭) | **정보** |

🔴 **I1 은 처음 쓴 형태가 동어반복이었다.** 작년 스프레드시트에서 `낙찰가 합 + 잔액 = $200`
이 유효한 검산이었던 이유는 진행자가 **두 숫자를 각각 손으로 적었기** 때문이다. 이 툴은
잔액을 지출에서 파생하므로(`budget = 200 − spent`) 그 등식은 **항상 참**이고, 영원히 ✅ 만
띄우는 장식이 된다. 실제로 깨질 수 있는 것은 **두 표현의 불일치**(`teams[].picks` ↔ `log` —
철회 버그·부분 저장·수기 편집)와 **예산 초과**다. 그 둘을 서로 검산하게 바꿨다.
> 종이 검산을 코드로 옮길 때 **무엇이 독립적으로 기록된 값이었는지**를 봐야 한다.

**I5 를 정보 등급으로 둔 이유** (2026-09-01 사용자 피드백 *"포지션은 각자 알아서
관리할거같긴한데 보이니까 좋긴하네"*):

1. **포지션 관리는 각 팀 책임이고 포기도 전략이다** — 장부 오류가 아니다.
   진행자가 조치할 일이 아닌 것으로 배너를 노랗게 만들면 **진행자가 배너를 무시하게 되고,
   그 순간 I1~I4 경보도 같이 죽는다.** 경보의 가치는 그것이 드물 때만 유지된다.
2. 자격 데이터가 고르지 않다 (690명 중 야후 검증 19 · 큐레이션 155 · BBRef 단일 포지션
   추론 516). 실전 지명 풀 상위 174명은 전원 검증/큐레이션이지만 나머지는 추론이다.

따라서 배너는 **`장부 ✅ 4/4`** 로 하드 불변식만 세고, 포지션은 `· 포지션 1건` 으로
색 없이 덧붙인다. 팀 카드의 `C 없음` 플래그와 상세 패널 표시는 유지한다 — 보이는 건 좋다.
낙찰은 어떤 경우에도 막지 않는다.

---

## 3. 데이터

### 3-1. 재사용 자산 (전부 이미 존재 · 재수집 불필요)

| 자산 | 위치 | 용도 |
|---|---|---|
| BBRef 2025-26 per-game **582명** | `nba_fantasy_auction_2026/data/stats_2025_26/bbref/2025-26_per_game.csv` | **선수 풀 본체.** 13캣 원자료 완비 |
| 동 2024-25 **569명** | 같은 디렉토리 | 2025-26 결장자 보강 (Haliburton·Kyrie·Lillard·VanVleet). 합집합 **687명** |
| 실측 혼합 **171명** | `.../measured_full.json` | 정밀 스탯 우선 적용 |
| 시장가·my_max·tag **174명** | `nba_fantasy_auction_2026/data/players.json` | 시장가 밴드 참고 표시 |
| **한글 별칭 + 작년 낙찰가 120건** | `.../prior_auction_2025_26/results.csv` | **검색 사전 · "작년 $87" 표시** |
| 팀명 시드 12명 | `nba_draft/data/state.json` | 초기값 (2명은 사용자 입력) |

**스크래핑 코드는 전부 폐기한다.** `basketball_reference.py`의 위치 기반 컬럼 강제 대입
(`expected_columns` 순서 덮어쓰기)은 BBRef가 `Tm`→`Team`으로 바꾼 뒤 **조용히 오매핑될**
구조다. 이미 정제된 CSV가 옆 저장소에 있으므로 다시 만들 이유가 없다.

### 3-2. 빌드

`build_db.py` — 1회 실행, stdlib `csv`/`json`만 사용 (의존성 0)

```
2025-26 per_game (583)
  ← measured_full.json (171) 덮어쓰기
  ← players.json (174) 시장가·tag 병합
  ← results.csv (120) 한글 별칭 · 작년가 병합
  ← 2026 신인 수동 3명 (Dybantsa WAS · Peterson UTA · Boozer MEM — NBA 실적 없음)
  → players.min.json → console.html 에 인라인 임베드
```

임베드는 `nba_fantasy_auction_2026/tool/tool_embed.py` 패턴 차용 → **단일 파일 유지**.

---

## 4. 일정 — 작업 16일 / 잔여 34일

| 단계 | 기간 | 산출물 | 완료 판정 |
|---|---|---|---|
| ~~**P0** 데이터~~ | ~~D+2~~ **완료 2026-09-01** | `build_db.py` · `players.min.json` (122KB) | ✅ **8/8 통과** — 690명 · 별칭 147명 · 별칭 히트율 100% |
| ~~**P1** 빠른 기록 MVP~~ | ~~D+5~~ **완료 2026-09-01** | `console.src.html` + `embed.py` → `console.html` (269KB) · `tests/` 4종 | ✅ **87/87 테스트** · 사적 정보 유출 0건 · 작년 120건 타수 측정 **건당 6.6타 = 1.6초** (목표 5초) |
| ~~**P2** 규칙 엔진~~ | ~~D+3~~ **완료 2026-09-01** | 불변식 5종 + 상시 배너 + 상세 패널 · 포지션 이분 매칭 · 팀 카드 경고 플래그 | ✅ **146/146 테스트** — 위반 5종 전부 수동 주입 검출 |
| ~~**P3** 호가 모드 · 내보내기~~ | ~~D+3~~ **완료 2026-09-01** | 라이브 호가 · 10초 타이머 · 입찰 이력 영속화 · 결과 CSV · **지명 순서 당일 조정** | ✅ **335/335 테스트** · CSV 헤더·CRLF·행 형태 작년 파일과 대조 일치 |
| ~~**P4** 리허설~~ | ~~D+2~~ **완료 2026-09-01** | 리허설 빌드 경로 + `tests/rehearsal.mjs` 30건 | ✅ **120/120 재현** · 건당 **1.7초** · 총 **3.3분** · I1~I4 통과 · CSV 120건 대조 일치 |
| **P5** 인수인계 | D+1 | 사용법 1장 | **비행기 모드**에서 전 기능 동작 확인 |

P4가 회귀 테스트 겸 속도 측정을 동시에 한다 — 정답(작년 결과)이 있는 유일한 데이터셋이다.
D+16 완료 → 드래프트 2주 전, **예비 2주**.

---

## 4-1. P1 실측 (2026-09-01)

| 측정 | 값 |
|---|---|
| 작년 120건 총 타수 | **791타** (선수 2.0 · 팀 2.0 · 금액 2.6 + 확정) |
| 건당 | **6.6타 ≈ 1.6초** (4타/초 가정) |
| 126건 환산 | **831타 ≈ 3.5분** |
| 최악 케이스 | SGA 8타 · 그 외 전부 6~7타 |
| 검색 해결률 | 작년 120건 **120/120** |

**사적 정보 유출 정정 (2026-09-01, 사용자 지적)**

성적 카드를 만들면서 `시장 $90~102 · my_max $97` 을 함께 띄웠다 — 0절 경계를 내가 깼다.
데이터 층에서 제거했다:
- 레코드에서 `ml`·`mh`·`mymax`·`tag`·`inj` 삭제
- `w`(가중치) → `rk`(서수) 치환 — `w` 로 `market_high` 가 역산 가능했다
- 산출물 문구에서 사적 도구·전략 언급 제거 (`코어 플랜` `피벗` `태우기` `auction-console` 0건)
- 인수 조건 신설: **사적 판단 필드 유출 0건**

남은 레코드 필드는 11개뿐이다: `n t pos pos_raw kr line st gp src ly rk`.

**추가 반영 (2026-09-01, 사용자 요청)**
- 지명 규칙 **직전 낙찰자 → 순번 로테이션**. 만석 팀·이름 빈 팀은 자동 스킵하고
  상단에 다음 4명 대기열을 띄운다. 저장된 v1 세션은 v2 로 자동 이관된다.
- **13캣 성적 카드** 신설. 종전에는 PTS/REB/AST 3개만 저장돼 있었다 →
  `players.min.json` 에 13캣 전 항목 + MPG/GP/A/T/DD 를 실었다 (122KB → 233KB).

타수 측정에서 두 건이 잡혀 그 자리에서 고쳤다:
- 라틴 별칭(`SGA` `KAT` `KD`)이 **완전일치만** 매칭돼 접두 입력이 안 들었다 → 접두 매칭 추가
- `commit()`이 `pick` 없이 호출되면 예외로 죽었다 (UI 경로로는 도달 불가지만) → 가드 추가

## 4-2. P2 실측 (2026-09-01)

`tests/p2.mjs` 45건. 인수 조건이 "위반 케이스 수동 주입 시 전부 검출"이므로, 각 불변식마다
**커밋 경로로는 만들 수 없는 훼손 상태를 직접 주입**해 검출을 확인했다.

| 불변식 | 주입한 훼손 | 검출 |
|---|---|---|
| I1 | 로스터 금액만 변경 · 유령 선수 추가 · 지출 $250 | ✅ 3종 |
| I2 | 10번째 선수 직접 push | ✅ |
| I3 | 당시 상한 초과로 조작 · $0 · **로그 순서 뒤바꿈** | ✅ 3종 |
| I4 | 같은 선수 로그 중복 push | ✅ |
| I5 | 가드 9명 만석 · 가드 7명(2+2<5 이미 불가능) | ✅ 2종 (정보 등급) |

I3 테스트에 **순서 뒤바꾸기**를 넣은 이유: 금액만 검사하면 통과하고 시점을 검사하면
위반이 되는 케이스라, 재생이 실제로 시점을 본다는 증거가 된다.

포지션 이분 매칭(Kuhn) 단위 검증도 포함 — 가드 3명→2슬롯 · 5포지션 정확히→5슬롯 ·
교차 배정 필요한 조합→5슬롯 · 포지션 미상→0슬롯.

## 4-3. P3 실측 (2026-09-01)

**지명 순서 당일 조정** (사용자 요청: *"지명 순서는 그날 정해질것같은데 조정 가능한가"*)

빌드 시 `teams.json` 배열 순으로 고정돼 있었다 → 상태 모델을 분리했다.
`S.order`(팀 인덱스 나열) + `S.nomPos`(그 안의 위치). v2 → v3 마이그레이션 포함.

입력 경로는 **접두사 나열**이다 — 추첨 결과를 말로 듣고 바로 친다:
```
병 준 진 경 원 윤 단 지 두 수 철 정 강 완
```
모호하면 후보를 보여주고 거부하고(`"지" 가 지원·지훈 중 모호하다`), 빠진 팀도 지목한다.
↑↓ 미세 조정과 "현재 지명 위치" 지정도 있다. **진행 중 순서를 바꿔도 낙찰 이력은 불변**
(테스트로 고정). `normOrder()` 가 중복·범위 초과·비정수를 항상 유효한 순열로 복구한다.

**라이브 호가 모드** (Tab 전환)

| 항목 | 구현 |
|---|---|
| 자동 $1 입찰 | 지명 즉시 지명자 이름으로 걸린다 (리그 규칙) |
| 입찰 입력 | `병34` ⏎ — 팀 접두사 + 금액을 붙여 쓴다. 한글에 숫자가 없어 파싱이 모호하지 않다 |
| 타이머 | 10초 · 새 입찰마다 리셋 · 3·2·1초 비프 · 0초에 낮은 톤 |
| 낙찰 | **빈 입력에 Enter** — 현재 최고가/최고입찰자로 확정 |
| 입찰 이력 | 낙찰 레코드에 박아 영속화 — 구 Streamlit 판은 메모리에만 있어 재시작 시 소실됐다(B6) |
| 소리 | WebAudio 로 합성. 오디오 파일 없음 → 오프라인 자립 유지 |

**발견성 정정 2건** (사용자 피드백):
1. *"타이머가 어디있어?"* — 렌더는 정상이었고 조건이 두 개 겹쳐 안 보였다(Tab + 지명).
   모드 표시를 작은 회색 칩 → **클릭 가능한 버튼 `모드: 빠른 기록 [Tab]`** 으로,
   라이브 전환 즉시 `지명 대기 ⏱ 10s` 패널을 띄우고, 빠른 기록 힌트에 Tab 안내를 넣었다.
2. *"선수지명한다음에 뭐해야되는지 잘안보인다"* — 정보는 4곳(스테이지·placeholder·hint·results)에
   흩어진 작은 글씨였고 화면이 **상태만 크게** 보여줬다. 지명 후 좌측을 **다음 동작 3줄**로
   재구성했다. 핵심은 추상적 안내("빈 입력 Enter = 낙찰") 대신 **구체적 대상 명시**
   (`병욱 에게 $34 확정`)이고, 입찰 줄은 타이핑을 실시간 파싱해 되비춘다.
   `parseLiveBid()` 를 추출해 키 처리와 미리보기가 같은 규칙을 쓴다.

🔴 **타이머가 0에 닿아도 자동 낙찰하지 않는다.** 규칙은 10초 자동 마감이지만
**오낙찰이 무입찰보다 비싸다.** 0초에 "낙찰 대기"를 크게 띄우고 진행자의 Enter 를 기다린다.
(테스트: `만료돼도 자동 낙찰하지 않는다`)

**결과 CSV — 바이트 호환 검증**

작년 파일을 테스트에서 실제로 읽어 대조했다:
- 헤더 `manager,name_kr,name_en,price,read_confidence` **문자열 일치**
- **CRLF** 종결 · LF 단독 0건
- 행 형태 대조: 우리 출력 `병욱,웸비,Victor Wembanyama,87,high` = 작년 파일 2행과 동일
- `read_confidence` 는 전부 `high` — 사진 판독 확신도였고 이 툴은 직접 기록한다
- 쉼표·따옴표 포함 팀명 인용 처리

이 파일은 차년도 `prior_auction_<season>/results.csv` 로 그대로 들어간다.
읽는 쪽이 8곳(`tool/*.py` 7 + `validate.py`)이라 스키마를 바꾸면 그쪽이 깨진다.

## 4-4. 내장 사용법 (2026-09-01, 사용자 요청 *"설명이 좀 들어가있으면 좋겠다"*)

`console.html` 안에 7개 절의 사용법을 넣었다. 진행자가 다른 사람일 수 있으므로
**README 없이 파일만 열어도 쓸 수 있어야** 한다.

| 절 | 다루는 것 |
|---|---|
| 1 | 빠른 기록 3단계 · 별칭/초성 검색 · 임시 등재 |
| 2 | 라이브 호가 · 붙여쓰기 입찰 · **자동 낙찰 안 하는 이유** |
| 3 | 시간 되돌리기(Ctrl+Z) vs 개별 철회([취소]) 구분 |
| 4 | 순번 로테이션 · 당일 추첨 결과 입력법 |
| 5 | 툴이 강제하는 것 · 장부 배너 의미 · 포지션은 막지 않음 |
| 6 | **성적 카드 읽기** — OREB/A/T/DD 약어 · TOV 색 · `—` 의 뜻 · 출처 표기 3종 |
| 7 | 자동 저장 · 세션/CSV 내보내기 · 오프라인 |

- **첫 실행 시 자동으로 뜬다** (`helpSeen` 플래그). "자동으로 띄우지 않기" 체크로 끌 수 있다.
- `?` · `F1` · 상단 `사용법 ?` 버튼으로 열고 `Esc` 로 닫는다.
- `tests/help.mjs` 38건이 **내용 커버리지를 문자열로 검증**한다 — 25개 필수 항목이
  실제 마크업에 있는지, 그리고 **사적 정보(시장가·my_max·코어 플랜·피벗·태우기)가
  설명문에 섞여 들어가지 않았는지**까지 본다.

## 4-5. P4 리허설 실측 (2026-09-01)

### 규격 문제 해결 — 제품을 건드리지 않았다

작년 12팀×10인 vs 올해 14팀×9인. 리그 규격은 임베드 데이터(`DB.league`)에서 오므로
**빌드 시 오버라이드**하는 것이 정확한 이음새다. 실전 산출물은 그대로 둔다.

```
python3 build_db.py --teams 12 --slots 10 --seed-from-prior \
    --out data/players.rehearsal.json
python3 embed.py --data data/players.rehearsal.json --out console.rehearsal.html
```

`--seed-from-prior` 는 팀 시드를 작년 참가자 12명으로 대체한다.

### 순서 독립성 — 낙찰 순서가 기록에 없어도 재현 가능하다

작년 사진에 **낙찰 순서·지명자가 없다**(`prior_auction_2025_26/README.md`). I3 는 "그 시점의
최대입찰가"를 보므로 순서가 필요해 보이지만, 실제로는 불필요하다:

> `maxBid = 잔액 − (남은 슬롯 − 1)` 이므로 가격 $p 를 남은 슬롯 k 개일 때 사려면
> `지출누적 + p + (k−1) ≤ 200` 이어야 한다. 남은 k−1 건이 각각 $1 이상이므로
> `지출누적 + p + (k−1) ≤ 총지출 ≤ 200`. **총지출이 예산 이내면 어떤 순서로도 통과한다.**

작년 12팀 전부 지출 ≤ $200 이므로 파일 순서(가격 내림차순) 재생으로 충분하다.

### 결과

| 항목 | 값 |
|---|---|
| 재입력 | **120/120 전량 낙찰** · 실패 0 |
| 팀별 대조 | 12팀 인원·지출 **전부 일치** · 총 지출 $2,368 |
| 검산 | 전 팀 `지출 + 잔액 = $200` · **$1 남긴 팀 10곳** (작년 실제 패턴) |
| 불변식 | I1~I4 통과 (I5 포지션: 두현 SF 부족 — 정보 등급) |
| 만석 후 | 지명자 `null` · 대기열 공백 · 121번째 거부 |
| CSV | 120건 `(팀\|선수\|금액)` **대조 일치** · 헤더 동일 |
| 속도 | 총 **794타** · 건당 **6.6타 = 1.7초** · 120건 **3.3분** (목표 5초/10분) |
| 120건 상태 되돌리기 | 개별 철회 → 예산 복구 → `Ctrl+Z` → **상태 완전 복원** |

### 🔴 리허설이 잡은 실제 버그 — 표시명 우선순위

CSV 대조에서 3건이 틀렸다. 처음엔 발음기호 문제로 보였으나 반대였다.

| 선수 | BBRef | `players.json` (소비자 DB) | 작년 파일 |
|---|---|---|---|
| Porziņģis | `Kristaps Porziņģis` | `Kristaps Porziņģis` | `Kristaps Porzingis` |
| Vučević | `Nikola Vučević` | `Nikola Vučević` | `Nikola Vucevic` |
| **Butler** | **`Jimmy Butler`** | **`Jimmy Butler III`** | `Jimmy Butler III` |

`build_db.py` 가 표시명을 **BBRef 우선**으로 뽑고 있었다. 앞 둘은 소비자
정규화(`NFKD → ascii → lower`)가 흡수하지만, **그 정규화는 접미사를 접지 않는다** —
`jimmy butler ≠ jimmy butler iii` 로 **조인이 조용히 실패**한다.

→ 우선순위를 **큐레이션 풀 > BBRef** 로 뒤집고, 인수 조건을 신설했다:
**"풀 선수 표시명이 players.json 과 정확히 일치"** (174/174 통과).

> 스키마만 맞추면 호환이 아니다. **조인 키의 표기까지 맞아야** 호환이다.
> 이건 작년 데이터로 실제 왕복을 돌려보지 않으면 드러나지 않는 종류의 오류다.

## 5. 폐기 목록

P5에서 일괄 정리한다 (P2~P4 동안 참조 가능성을 남겨둔다).

```
삭제: app.py · pages/ · src/utils/{app 전부} · uv.lock · data/state.json
의존성 제거: streamlit · plotly · nba-api · beautifulsoup4 · lxml · pillow · pandas · requests
남김: prd.md · CLAUDE.md (재작성) — 팀명 시드는 data/teams.json 으로 이전 완료
신규: console.html · build_db.py · players.min.json · README.md
```

최종 의존성 **0개**. `pyproject.toml`도 불필요해진다.

git 정리 필요 — `CLAUDE.md`·`prd.md`·`data/state.json`·결과 CSV가 현재 **untracked**.

---

## 6. 위험과 대응

| # | 위험 | 대응 |
|---|---|---|
| ~~R1~~ | ~~14팀 중 2명 미정~~ | ✅ **종결 2026-09-01** — 13·14번 **강호·완식** 확정. 시드는 `data/teams.json`(정본, state.json 비의존). 화면 편집 경로는 유지 |
| R2 | BBRef에 **2026 신인 3명 없음** — Dybantsa·Peterson·Boozer (Flagg·Edgecombe·Knueppel은 2025 드래프트조로 실적 있음) | `players.json` 시장가로 수동 등재 ✅ P0 완료. 지명 시 미발견 대비 **"임시 선수 추가"** 입력 경로는 P1 필수 |
| R3 | **진짜 커미셔너가 다른 사람** | 단일 HTML이라 파일 1개 전달로 끝. 단 P4 리허설을 **커미셔너와 함께** 해야 실전 채택됨 |
| R4 | 실전에 창 2개(이 툴 + auction-console) | 역할 분리 명문화(0절). 연동은 **P3에서 단방향 수동**: 이 툴이 클립보드 JSON 내보내기 → 피벗 판단 시점에만 붙여넣기. `file://` 오리진이 달라 localStorage 공유는 불가 |
| R5 | 스코프 크리프 (밸류·추천 기능 유입) | 0절 경계 위반은 즉시 거부. 이 툴은 **장부**다 |
| 🔴 **R7** | **지명 규칙 변경이 형제 저장소 5곳을 무효화** — `data/league_settings.json` · `docs/01-league-and-format.md:12` · **`docs/02-auction-strategy.md:53` (§3 "직전 낙찰자 룰 활용" — 전략 절 하나가 통째로)** · `docs/12-auction-playbook.md:568` · `README.md:52`. `tool/auction-console.html` 의 "태우기 지명 순서" 기능도 이 전제 위에 있다 | **이 저장소 밖의 일이라 손대지 않았다.** 순번 로테이션은 지명 타이밍을 스스로 못 고르므로 태우기 전략의 가정이 달라진다 → 드래프트 전 형제 저장소에서 별도 정정 필요 |
| R6 | DD(더블더블) 캣 | `measured_full.json`에 171명 추정치가 있으나(`DD_est_season`) 장부에는 불필요 → **의도적 생략**. 캣 판단은 auction-console 몫 |

---

## 7. 성공 기준 — 하나뿐

> **2026-10-05 드래프트에서 실제로 쓰였는가.**

측정 가능한 대리 지표: P4 리허설에서 **작년 120건을 10분 내 무오류 재입력**.
이걸 통과하면 스프레드시트를 대체할 수 있다. 못 하면 또 안 쓰인다.
